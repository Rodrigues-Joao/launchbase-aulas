{% extends "layout.njk" %}

{% block content %}
<div class="card table-container">
    <a href="/students/create">Novo</a>
   <table>
       <thead>
           <tr>
               <th>Estudantes</th>
               <th>Carga Horária</th>
               <th>Action</th>
           </tr>
       </thead>
       <tbody>
            {% for student in students %}
                <tr>
                    <td>
                        <div>
                            <span 
                                style="background-image: url({{student.avatar_url}})">
                            </span>
                            {{student.name}}
                        </div>
                    </td>
                    <td > 
                        <div class="workload">
                            <span class="span-classes">{{student.workload}}</span>
                        
                        </div>
                    </td>
                    <td>
                        <a href="/students/{{student.id}}">ver</a>
                    </td>
                </tr>
           {% endfor%}
       </tbody>
   </table>
</div>
{% endblock content %}
